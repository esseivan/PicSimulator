<Window x:Class="PicSimulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PicSimulator"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:lib="clr-namespace:PicSimulatorLib;assembly=PicSimulatorLib"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
	<DockPanel>
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="_File">
				<MenuItem Header="_Open" Click="MenuItemOpen_Click"/>
				<MenuItem Header="_Quit"/>
			</MenuItem>
		</Menu>
		<Grid>
			<!--<ListBox x:Name="TestList" SelectionMode="Multiple">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<Border BorderBrush="LightGray" BorderThickness="0">
							<StackPanel Orientation="Vertical">
								<StackPanel Orientation="Horizontal" Margin="0">
									<TextBlock Text="{Binding Address}" TextAlignment="Right" Width="30" Margin="0 0 5 0"/>
									<Border BorderThickness="1" BorderBrush="LightGray" Width="1" />
									<TextBlock Text="{Binding Value, StringFormat=X4}" Margin="5 0 5 0" Width="30"/>
									<Border BorderThickness="1" BorderBrush="LightGray" Width="1" />
									<TextBlock Text="{Binding}" Margin="5 0 0 0" Width="100"/>
								</StackPanel>
								<Border BorderBrush="LightGray" BorderThickness="1" Height="1" />
							</StackPanel>
						</Border>
					</DataTemplate>
				</ListBox.ItemTemplate>
				<ListBox.Items>
					<sys:String Value="Offset">Addr Offset</sys:String>
					<lib:Instruction Value="0x3180" Address="0"/>
					<lib:Instruction Value="0x3180" Address="0"/>
					<lib:Instruction Value="0x3180" Address="1"/>
					<lib:Instruction Value="0x3180" Address="2"/>
					<lib:Instruction Value="0x3180" Address="3"/>
					<lib:Instruction Value="0x3180" Address="4"/>
				</ListBox.Items>
			</ListBox>
			-->

			<ListView x:Name="listCode" SelectionMode="Single" ItemsSource="{Binding instructions}" FontFamily="Courier New">
				<ListView.View>
					<GridView>
						<GridView.Columns>
							<GridViewColumn>
								<GridViewColumn.CellTemplate>
									<DataTemplate>
										<TextBlock Text="{Binding Address}"/>
									</DataTemplate>
								</GridViewColumn.CellTemplate>
								<GridViewColumn.Header>
									<TextBlock Text="Column 1" />
								</GridViewColumn.Header>
							</GridViewColumn>
							<GridViewColumn>
								<GridViewColumn.CellTemplate>
									<DataTemplate>
										<TextBlock Text="{Binding Value, StringFormat=X4}"/>
									</DataTemplate>
								</GridViewColumn.CellTemplate>
								<GridViewColumn.Header>
									<TextBlock Text="Column 2" />
								</GridViewColumn.Header>
							</GridViewColumn>
							<GridViewColumn>
								<GridViewColumn.CellTemplate>
									<DataTemplate>
										<TextBlock Text="{Binding}" />
									</DataTemplate>
								</GridViewColumn.CellTemplate>
								<GridViewColumn.Header>
									<TextBlock Text="Column 3" />
								</GridViewColumn.Header>
							</GridViewColumn>
						</GridView.Columns>
					</GridView>
				</ListView.View>
			</ListView>
		</Grid>
	</DockPanel>
</Window>
